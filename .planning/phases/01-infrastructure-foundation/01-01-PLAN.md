---
phase: 01-infrastructure-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - nutrihelp_app/pyproject.toml
  - nutrihelp_app/manage.py
  - nutrihelp_app/control/__init__.py
  - nutrihelp_app/control/celery.py
  - nutrihelp_app/control/settings.py
  - nutrihelp_app/control/urls.py
  - nutrihelp_app/control/wsgi.py
  - dotenv_files/.env-example
  - .gitignore
autonomous: true
requirements: [INFRA-01, INFRA-02]
must_haves:
  truths:
    - "O núcleo Django (manage.py, control/) existe e é importável com DJANGO_SETTINGS_MODULE=control.settings"
    - "pyproject.toml declara todas as 20+ dependências do projeto (D-03/D-04) sem nenhuma faltando"
    - "settings.py falha imediatamente se SECRET_KEY não estiver definida (sem default no código)"
    - "O app Celery 'nutrihelp' é carregado quando Django inicia (control/__init__.py importa celery_app)"
    - "dotenv_files/.env está no .gitignore; uv.lock NÃO está no .gitignore"
  artifacts:
    - path: "nutrihelp_app/pyproject.toml"
      provides: "Declaração completa de dependências (D-03/D-04)"
      contains: "drf-spectacular"
    - path: "nutrihelp_app/control/settings.py"
      provides: "Configuração Django única com DB, Celery, JWT, DRF, whitenoise"
      contains: "SECRET_KEY = config('SECRET_KEY')"
    - path: "nutrihelp_app/control/celery.py"
      provides: "App Celery 'nutrihelp' com autodiscover"
      contains: "Celery('nutrihelp')"
    - path: "nutrihelp_app/control/urls.py"
      provides: "URLs raiz (admin apenas em Phase 1)"
      contains: "admin.site.urls"
    - path: ".gitignore"
      provides: "Exclusão de segredos (D-09)"
      contains: "dotenv_files/.env"
  key_links:
    - from: "nutrihelp_app/control/__init__.py"
      to: "nutrihelp_app/control/celery.py"
      via: "from .celery import app as celery_app"
      pattern: "from .celery import app"
    - from: "nutrihelp_app/control/settings.py"
      to: "dotenv_files/.env"
      via: "python-decouple config()"
      pattern: "from decouple import config"
    - from: "nutrihelp_app/control/celery.py"
      to: "control.settings"
      via: "DJANGO_SETTINGS_MODULE"
      pattern: "control.settings"
---

<objective>
Criar o núcleo do projeto Django (`manage.py`, `control/`), o `pyproject.toml` completo com todas as dependências do projeto, e os arquivos de configuração de ambiente (`.env-example`, `.gitignore`). Nenhum app de negócio é criado nesta fase (D-01/D-02).

Purpose: Estabelecer o esqueleto Django que todas as fases seguintes preencherão e fixar todas as versões de dependências de uma vez (D-03/D-04 — `uv.lock` gerado uma única vez na Phase 1 e nunca tocado depois).
Output: Diretório `nutrihelp_app/control/` completo, `pyproject.toml`, `.env-example` documentado e `.gitignore` expandido.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-infrastructure-foundation/01-CONTEXT.md
@.planning/phases/01-infrastructure-foundation/01-RESEARCH.md
@.planning/phases/01-infrastructure-foundation/01-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- Valores canônicos exatos a copiar verbatim. NÃO inventar nem simplificar. -->

pyproject.toml dependencies (D-04 — todas obrigatórias):
django>=5.2,<6.0, djangorestframework>=3.17,<4.0, djangorestframework-simplejwt>=5.5,<6.0,
psycopg2-binary>=2.9,<3.0, pgvector>=0.4,<0.5, langchain>=1.3,<2.0,
langchain-text-splitters>=1.1,<2.0, langchain-openai>=1.2,<2.0, langchain-anthropic>=1.4,<2.0,
langchain-community>=0.4,<0.5, anthropic>=0.103,<1.0, openai>=2.37,<3.0,
celery>=5.6,<6.0, redis>=7.4,<8.0, chromadb>=0.5,<1.0, pdfplumber>=0.11,<0.12,
pytesseract>=0.3,<0.4, pillow>=12,<13, python-decouple>=3.8, whitenoise>=6.12,<7.0,
gunicorn>=26,<27, drf-spectacular>=0.29,<0.30

Conflito resolvido (CLAUDE.md vence sobre guia_implementacao.md):
- Diretório: nutrihelp_app/ (NÃO nutrichat/)
- Módulo config: control/ (NÃO config/)
- Settings: único control/settings.py (NÃO split base/dev/prod)
- Celery app name: 'nutrihelp' (NÃO 'nutrichat', NÃO 'control')
- JWT: access 5min, refresh 1 dia (NÃO 1h/7d do guia)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar pyproject.toml completo e .gitignore</name>
  <files>nutrihelp_app/pyproject.toml, .gitignore</files>
  <read_first>
    - .planning/phases/01-infrastructure-foundation/01-PATTERNS.md (seção pyproject.toml linhas 281-335 e seção .gitignore linhas 651-695)
    - .planning/phases/01-infrastructure-foundation/01-RESEARCH.md (seção pyproject.toml completo linhas 128-178)
    - .gitignore (estado atual — contém apenas "# Nutrihelp" e "guia_implementacao.md")
    - CLAUDE.md (seção Dependency Management — uv + pyproject.toml + uv.lock)
  </read_first>
  <action>
Criar `nutrihelp_app/pyproject.toml` com EXATAMENTE este conteúdo (D-03/D-04 — todas as dependências de uma vez, sem omitir nenhuma):

```toml
[project]
name = "nutrihelp"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    # Framework
    "django>=5.2,<6.0",
    "djangorestframework>=3.17,<4.0",
    "djangorestframework-simplejwt>=5.5,<6.0",
    # Database
    "psycopg2-binary>=2.9,<3.0",
    "pgvector>=0.4,<0.5",
    # RAG / LangChain (1.x)
    "langchain>=1.3,<2.0",
    "langchain-text-splitters>=1.1,<2.0",
    "langchain-openai>=1.2,<2.0",
    "langchain-anthropic>=1.4,<2.0",
    "langchain-community>=0.4,<0.5",
    # LLM SDKs diretos
    "anthropic>=0.103,<1.0",
    "openai>=2.37,<3.0",
    # Tarefas assíncronas
    "celery>=5.6,<6.0",
    "redis>=7.4,<8.0",
    # Vectorstore local dev
    "chromadb>=0.5,<1.0",
    # Processamento de documentos
    "pdfplumber>=0.11,<0.12",
    "pytesseract>=0.3,<0.4",
    "pillow>=12,<13",
    # Configuração
    "python-decouple>=3.8",
    # Servidor e static files
    "whitenoise>=6.12,<7.0",
    "gunicorn>=26,<27",
    # API docs (Phase 7)
    "drf-spectacular>=0.29,<0.30",
]
```

Reescrever `.gitignore` (preservando a linha `guia_implementacao.md` existente) para incluir, no mínimo, estas entradas. IMPORTANTE: `uv.lock` NÃO deve aparecer no .gitignore — deve ser commitado para builds reproduzíveis (RESEARCH.md §Pitfall 3):

```gitignore
# Nutrihelp
guia_implementacao.md

# Secrets — nunca commitar (D-09)
dotenv_files/.env

# Static files coletados
nutrihelp_app/staticfiles/

# Python cache
__pycache__/
*.pyc
*.pyo
*.pyd
.Python

# Virtual environments
.venv/
venv/
env/

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Logs
*.log
```
  </action>
  <verify>
    <automated>grep -q 'drf-spectacular>=0.29' nutrihelp_app/pyproject.toml && grep -q 'langchain-text-splitters>=1.1' nutrihelp_app/pyproject.toml && grep -q 'dotenv_files/.env' .gitignore && ! grep -q '^uv.lock' .gitignore && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `nutrihelp_app/pyproject.toml` existe e contém `name = "nutrihelp"`
    - `grep -c '>=' nutrihelp_app/pyproject.toml` retorna >= 20 (todas as deps de D-04 presentes)
    - `pyproject.toml` contém literalmente `langchain-text-splitters>=1.1,<2.0` (pacote separado em LangChain 1.x)
    - `pyproject.toml` contém `drf-spectacular>=0.29,<0.30`
    - `.gitignore` contém a linha `dotenv_files/.env`
    - `.gitignore` contém `nutrihelp_app/staticfiles/`
    - `.gitignore` NÃO contém nenhuma linha começando com `uv.lock`
    - A linha `guia_implementacao.md` permanece presente no `.gitignore`
  </acceptance_criteria>
  <done>pyproject.toml com 20+ dependências exatas e .gitignore expandido com dotenv_files/.env excluído e uv.lock mantido fora da exclusão.</done>
</task>

<task type="auto">
  <name>Task 2: Criar núcleo Django (manage.py, control/) com settings, celery, urls, wsgi</name>
  <files>nutrihelp_app/manage.py, nutrihelp_app/control/__init__.py, nutrihelp_app/control/celery.py, nutrihelp_app/control/settings.py, nutrihelp_app/control/urls.py, nutrihelp_app/control/wsgi.py</files>
  <read_first>
    - .planning/phases/01-infrastructure-foundation/01-PATTERNS.md (seções manage.py linhas 358-388, celery.py linhas 392-417, __init__.py linhas 420-430, settings.py linhas 434-563, urls.py linhas 567-588, wsgi.py linhas 591-602)
    - .planning/phases/01-infrastructure-foundation/01-RESEARCH.md (Pattern 4 settings linhas 442-493, Pattern 5 celery linhas 495-510)
    - CLAUDE.md (seções Custom User Model, Authentication System, API Structure, Environment Variables)
  </read_first>
  <action>
Criar todos os arquivos do núcleo Django seguindo os padrões canônicos do PATTERNS.md verbatim. Nenhum app de negócio (D-01/D-02).

`nutrihelp_app/manage.py` — template Django 5.2 padrão com `os.environ.setdefault('DJANGO_SETTINGS_MODULE', 'control.settings')` (PATTERNS.md linhas 363-385).

`nutrihelp_app/control/__init__.py` — carrega o app Celery:
```python
from .celery import app as celery_app

__all__ = ('celery_app',)
```

`nutrihelp_app/control/celery.py` — app Celery nomeado `'nutrihelp'` (NÃO 'nutrichat', NÃO 'control' — Pitfall 5):
```python
import os
from celery import Celery

os.environ.setdefault('DJANGO_SETTINGS_MODULE', 'control.settings')

app = Celery('nutrihelp')
app.config_from_object('django.conf:settings', namespace='CELERY')
app.autodiscover_tasks()
```

`nutrihelp_app/control/wsgi.py`:
```python
import os
from django.core.wsgi import get_wsgi_application

os.environ.setdefault('DJANGO_SETTINGS_MODULE', 'control.settings')

application = get_wsgi_application()
```

`nutrihelp_app/control/urls.py` — apenas admin em Phase 1 (os prefixos de API ficam comentados como guia para fases seguintes):
```python
from django.contrib import admin
from django.urls import path

urlpatterns = [
    path('admin/', admin.site.urls),
    # Fases seguintes:
    # path('api/accounts/', include('accounts.urls')),
    # path('api/patients/', include('patients.urls')),
    # path('api/documents/', include('documents.urls')),
    # path('api/chat/', include('chat.urls')),
]
```

`nutrihelp_app/control/settings.py` — copiar EXATAMENTE o conteúdo canônico de PATTERNS.md linhas 444-549. Pontos não-negociáveis:
- `SECRET_KEY = config('SECRET_KEY')` SEM default (D-09 — falha se ausente)
- `INSTALLED_APPS` inclui `rest_framework`, `rest_framework_simplejwt.token_blacklist`, `drf_spectacular`
- `MIDDLEWARE` tem `whitenoise.middleware.WhiteNoiseMiddleware` logo após `SecurityMiddleware`
- `ROOT_URLCONF = 'control.urls'`, `WSGI_APPLICATION = 'control.wsgi.application'`
- `DATABASES` via config() com `POSTGRES_HOST` default `nutrihelp_postgres`
- `CELERY_BROKER_URL = config('REDIS_URL', default='redis://nutrihelp_redis:6379/0')`
- `SIMPLE_JWT` com `ACCESS_TOKEN_LIFETIME=timedelta(minutes=5)`, `REFRESH_TOKEN_LIFETIME=timedelta(days=1)`, `ROTATE_REFRESH_TOKENS=True`, `BLACKLIST_AFTER_ROTATION=True`
- `REST_FRAMEWORK` com `JWTAuthentication`, `IsAuthenticated`, `DEFAULT_SCHEMA_CLASS='drf_spectacular.openapi.AutoSchema'`
- `STATIC_ROOT = BASE_DIR / 'staticfiles'`, `STATICFILES_STORAGE = 'whitenoise.storage.CompressedManifestStaticFilesStorage'`
- `LANGUAGE_CODE = 'pt-br'`, `TIME_ZONE = 'America/Sao_Paulo'`, `USE_TZ = True`
- `DEFAULT_AUTO_FIELD = 'django.db.models.BigAutoField'`
- Variáveis LLM/Vector store via config() (LLM_PROVIDER, OLLAMA_*, ANTHROPIC_API_KEY, OPENAI_API_KEY, VECTOR_STORE, CHROMA_*)

NÃO adicionar `default=` à SECRET_KEY sob nenhuma circunstância (anti-padrão crítico — viola D-09).
  </action>
  <verify>
    <automated>test -f nutrihelp_app/manage.py && grep -q "Celery('nutrihelp')" nutrihelp_app/control/celery.py && grep -q "SECRET_KEY = config('SECRET_KEY')" nutrihelp_app/control/settings.py && ! grep -q "config('SECRET_KEY', default" nutrihelp_app/control/settings.py && grep -q 'from .celery import app as celery_app' nutrihelp_app/control/__init__.py && grep -q 'admin.site.urls' nutrihelp_app/control/urls.py && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `nutrihelp_app/manage.py` contém `os.environ.setdefault('DJANGO_SETTINGS_MODULE', 'control.settings')`
    - `nutrihelp_app/control/celery.py` contém `Celery('nutrihelp')` e `app.autodiscover_tasks()`
    - `nutrihelp_app/control/__init__.py` contém `from .celery import app as celery_app`
    - `nutrihelp_app/control/wsgi.py` contém `get_wsgi_application` e `DJANGO_SETTINGS_MODULE`, `control.settings`
    - `nutrihelp_app/control/urls.py` contém `path('admin/', admin.site.urls)`
    - `settings.py` contém literalmente `SECRET_KEY = config('SECRET_KEY')` e NÃO contém `config('SECRET_KEY', default`
    - `settings.py` contém `rest_framework_simplejwt.token_blacklist` e `drf_spectacular` em INSTALLED_APPS
    - `settings.py` contém `whitenoise.middleware.WhiteNoiseMiddleware`
    - `settings.py` contém `ROOT_URLCONF = 'control.urls'` e `timedelta(minutes=5)`
    - `settings.py` contém `STATIC_ROOT = BASE_DIR / 'staticfiles'`
  </acceptance_criteria>
  <done>Núcleo Django completo: manage.py + control/{__init__,celery,settings,urls,wsgi}.py seguindo os padrões canônicos, SECRET_KEY sem default, Celery nomeado 'nutrihelp', apenas admin nas urls.</done>
</task>

<task type="auto">
  <name>Task 3: Criar dotenv_files/.env-example documentado</name>
  <files>dotenv_files/.env-example</files>
  <read_first>
    - .planning/phases/01-infrastructure-foundation/01-PATTERNS.md (seção .env-example linhas 606-647)
    - .planning/phases/01-infrastructure-foundation/01-RESEARCH.md (seção .env-example completo linhas 512-548)
    - CLAUDE.md (seção Environment Variables)
  </read_first>
  <action>
Criar `dotenv_files/.env-example` (commitado, placeholders) com TODAS as variáveis abaixo. Este arquivo é a fonte de verdade que o usuário copia para `dotenv_files/.env` (que é gitignored). Os nomes de host usam nomes de serviço Docker (D-08 — Redis/Postgres acessíveis só pela rede interna):

```bash
# Django
SECRET_KEY=troque-por-uma-chave-segura-e-longa
DEBUG=1
ALLOWED_HOSTS=localhost,127.0.0.1
CSRF_TRUSTED_ORIGINS=http://localhost:3600

# Banco de dados
DB_ENGINE=django.db.backends.postgresql
POSTGRES_DB=nutrihelp
POSTGRES_USER=nutrihelp_user
POSTGRES_PASSWORD=trocar-senha-segura
POSTGRES_HOST=nutrihelp_postgres
POSTGRES_PORT=5432

# Redis (broker Celery) — interno, sem porta exposta no host (D-08)
REDIS_URL=redis://nutrihelp_redis:6379/0

# LLM
LLM_PROVIDER=ollama
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
# NOTA: OPENAI_API_KEY é obrigatória para embeddings mesmo quando LLM_PROVIDER=anthropic

# Ollama (modelo deve ser baixado manualmente após docker compose up):
# docker exec nutrihelp_ollama ollama pull llama3.1:8b-instruct-q4_K_M
OLLAMA_BASE_URL=http://nutrihelp_ollama:11434
OLLAMA_MODEL=llama3.1:8b-instruct-q4_K_M

# Vector store
VECTOR_STORE=chroma
CHROMA_HOST=nutrihelp_chromadb
CHROMA_PORT=8000

# OCR (binário tesseract não incluído no Docker em Phase 1)
OCR_PROVIDER=tesseract
```

NÃO criar `dotenv_files/.env` (é gitignored e contém valores reais — responsabilidade do usuário copiar e preencher).
  </action>
  <verify>
    <automated>test -f dotenv_files/.env-example && grep -q '^SECRET_KEY=' dotenv_files/.env-example && grep -q 'POSTGRES_HOST=nutrihelp_postgres' dotenv_files/.env-example && grep -q 'REDIS_URL=redis://nutrihelp_redis:6379/0' dotenv_files/.env-example && grep -q 'OLLAMA_MODEL=llama3.1:8b-instruct-q4_K_M' dotenv_files/.env-example && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `dotenv_files/.env-example` existe
    - contém `SECRET_KEY=` (com placeholder, não vazio)
    - contém `POSTGRES_HOST=nutrihelp_postgres` (nome de serviço Docker, não localhost)
    - contém `REDIS_URL=redis://nutrihelp_redis:6379/0`
    - contém `OLLAMA_MODEL=llama3.1:8b-instruct-q4_K_M`
    - contém `LLM_PROVIDER=ollama`, `VECTOR_STORE=chroma`, `CHROMA_HOST=nutrihelp_chromadb`
    - `dotenv_files/.env` NÃO foi criado por este plano (apenas o .env-example)
  </acceptance_criteria>
  <done>.env-example completo com todas as variáveis do CLAUDE.md usando nomes de serviço Docker; .env real não criado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repositório git → histórico público | Segredos não podem ser commitados |
| Código fonte → runtime | SECRET_KEY deve vir do ambiente, nunca hardcoded |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | dotenv_files/.env | mitigate | `.gitignore` contém `dotenv_files/.env` (D-09); apenas `.env-example` com placeholders é commitado |
| T-01-02 | Information Disclosure | control/settings.py SECRET_KEY | mitigate | `SECRET_KEY = config('SECRET_KEY')` sem default — chave nunca aparece no código; ausência causa falha imediata (verificável: ausência de `default` no grep) |
| T-01-03 | Spoofing | ALLOWED_HOSTS / CSRF_TRUSTED_ORIGINS | accept | Em dev, hosts limitados a localhost via .env-example; hardening de headers HTTP é responsabilidade da Phase 7 (D-10) |
| T-01-04 | Tampering | pyproject.toml / uv.lock | mitigate | `uv.lock` commitado garante builds reproduzíveis; versões fixadas com ranges semver evitam upgrade silencioso (verificado no Dockerfile com `--frozen` no Plan 02) |
</threat_model>

<verification>
- `grep -c '>=' nutrihelp_app/pyproject.toml` >= 20
- `nutrihelp_app/control/` contém os 5 módulos (__init__, celery, settings, urls, wsgi)
- `settings.py` não tem default em SECRET_KEY
- `.gitignore` exclui dotenv_files/.env e mantém uv.lock fora da exclusão
- `.env-example` existe e usa nomes de serviço Docker
</verification>

<success_criteria>
- Núcleo Django scaffold completo e importável com DJANGO_SETTINGS_MODULE=control.settings (validação final ocorre no Plan 02 quando o container sobe)
- Todas as dependências de D-04 declaradas em pyproject.toml
- Configuração de segredos segura: SECRET_KEY sem default, .env gitignored
- INFRA-01 e INFRA-02: scaffold de aplicação pronto para o stack Docker do Plan 02
</success_criteria>

<output>
After completion, create `.planning/phases/01-infrastructure-foundation/01-01-SUMMARY.md`
</output>
